# Git History

Generated by git-memory v0.1.0

## Commit b9e00dd: Initial commit

**Author:** Dominik Baran
**Date:** 2025-05-18 13:39:32
**Lines changed:** 207

```diff
diff --git a/.gitignore b/.gitignore
new file mode 100644
index 0000000..0a19790
--- /dev/null
+++ b/.gitignore
@@ -0,0 +1,174 @@
+# Byte-compiled / optimized / DLL files
+__pycache__/
+*.py[cod]
+*$py.class
+
+# C extensions
+*.so
+
+# Distribution / packaging
+.Python
+build/
+develop-eggs/
+dist/
+downloads/
+eggs/
+.eggs/
+lib/
+lib64/
+parts/
+sdist/
+var/
+wheels/
+share/python-wheels/
+*.egg-info/
+.installed.cfg
+*.egg
+MANIFEST
+
+# PyInstaller
+#  Usually these files are written by a python script from a template
+#  before PyInstaller builds the exe, so as to inject date/other infos into it.
+*.manifest
+*.spec
+
+# Installer logs
+pip-log.txt
+pip-delete-this-directory.txt
+
+# Unit test / coverage reports
+htmlcov/
+.tox/
+.nox/
+.coverage
+.coverage.*
+.cache
+nosetests.xml
+coverage.xml
+*.cover
+*.py,cover
+.hypothesis/
+.pytest_cache/
+cover/
+
+# Translations
+*.mo
+*.pot
+
+# Django stuff:
+*.log
+local_settings.py
+db.sqlite3
+db.sqlite3-journal
+
+# Flask stuff:
+i
... [truncated] ...
```

---

## Commit e887cb6: Create PROJECT.md

**Author:** Dominik Baran
**Date:** 2025-05-18 13:54:33
**Lines changed:** 185

```diff
@@ -1,184 +0,0 @@
-# **Project Name: `git-memory`**
-
-## **Tagline**
-
-> *AI-powered commit-by-commit memory and structure tracking for your Git projects.*
-
----
-
-## **Project Summary**
-
-`git-memory` is a Python CLI tool and library that walks through a Git repository’s history to generate a detailed memory (`memory.md`), a structural diagram (`structure.mmd`), and a patch (`history.md`) per commit (or commit group). 
-It integrates with AI providers (like OpenAI) to generate summaries and visual diagrams that reflect the evolution of the project.
-Each commit (or group of commits) gets:
-
-* a natural-language memory (`memory.md`)
-* a Mermaid diagram (`structure.mmd`)
-* a history file with the patches (`history.md`)
-
-Each commit (or related group of commits) for these files should capture:
-
-* **removal** of deprecated data.
-* **updates** made to existing data.
-* **addition** of new, meaningful content.
-
-This tool helps developers track how their code evolves, understa
... [truncated] ...
```

---

## Commit 58a15e6: Initial project scaffolding

**Author:** MasterGTFX
**Date:** 2025-05-18 14:18:57
**Lines changed:** 753

```diff
@@ -1,15 +1,174 @@
- # Byte-compiled / optimized / DLL files
- __pycache__/
- *.py[cod]
- # Distribution / packaging
- *.egg-info/
- dist/
- build/
- # Virtual environments
- .env
- .venv
- # History output
- .history/
- # Editor directories and files
- .vscode/
- .idea/
\ No newline at end of file
+# Byte-compiled / optimized / DLL files
+__pycache__/
+*.py[cod]
+*$py.class
+
+# C extensions
+*.so
+
+# Distribution / packaging
+.Python
+build/
+develop-eggs/
+dist/
+downloads/
+eggs/
+.eggs/
+lib/
+lib64/
+parts/
+sdist/
+var/
+wheels/
+share/python-wheels/
+*.egg-info/
+.installed.cfg
+*.egg
+MANIFEST
+
+# PyInstaller
+#  Usually these files are written by a python script from a template
+#  before PyInstaller builds the exe, so as to inject date/other infos into it.
+*.manifest
+*.spec
+
+# Installer logs
+pip-log.txt
+pip-delete-this-directory.txt
+
+# Unit test / coverage reports
+htmlcov/
+.tox/
+.nox/
+.coverage
+.coverage.*
+.cache
+nosetests.xml
+coverage.xml
+*.cover
+*.py,co
... [truncated] ...
```

---

## Commit a1a2ed0: refactor: Implement commit grouping and skipping logic

**Author:** dominIk (aider)
**Date:** 2025-05-18 14:59:45
**Lines changed:** 111

```diff
@@ -1,84 +1,38 @@
 """
-History generation for git-memory.
+History generation stub for git-memory.
 """
 
 import os
-from git import Repo, Commit
+from git import Repo
 
 def generate_history(repo_path: str, min_commits: int, min_diff_lines: int | None) -> None:
     """
-    Walk through commits and generate history files for each commit group.
-    Groups commits by min_commits. Skips processing if the group's directory exists.
+    Walk through commits and generate stub history files for each commit.
     """
     repo = Repo(repo_path)
     history_dir = os.path.join(os.getcwd(), ".history")
     os.makedirs(history_dir, exist_ok=True)
 
-    # Iterate through commits in reverse chronological order (HEAD first)
-    # We get all commits first to easily check for the last commit in the repo
-    all_commits = list(repo.iter_commits())
+    commits = list(repo.iter_commits(max_count=min_commits))
+    for commit in commits:
+        commit_dir = os.path.join(history_dir, commit.hex
... [truncated] ...
```

---

## Commit 3801f29: chore: Remove history generation logic from history.py

**Author:** dominIk (aider)
**Date:** 2025-05-18 15:10:59
**Lines changed:** 86

```diff
@@ -1,5 +1,64 @@
-            # Calculate the diff from the parent of the oldest commit to the newest commit in the group.
-            # If parent_of_group is None (initial commit group), diff is from empty tree to first_commit_in_group.
+"""
+History generation for git-memory.
+"""
+
+import os
+from git import Repo, Commit
+
+def generate_history(repo_path: str, min_commits: int, min_diff_lines: int | None) -> None:
+    """
+    Walk through commits and generate history files for each commit group.
+    Groups commits by min_commits. Skips processing if the group's directory exists.
+    """
+    repo = Repo(repo_path)
+    history_dir = os.path.join(os.getcwd(), ".history")
+    os.makedirs(history_dir, exist_ok=True)
+
+    # Iterate through commits in reverse chronological order (HEAD first)
+    # We get all commits first to easily check for the last commit in the repo
+    all_commits = list(repo.iter_commits())
+
+    commit_group = []
+    # Iterate through commits from newe
... [truncated] ...
```

---

## Commit 6457126: refactor: Rename min_commits to commits_per_group for clarity

**Author:** dominIk (aider)
**Date:** 2025-05-18 15:13:02
**Lines changed:** 140

```diff
@@ -9,19 +9,19 @@ app = typer.Typer(
 @app.command()
 def main(
     repo_path: str = typer.Argument(..., exists=True, file_okay=False, dir_okay=True, help="Path to the Git repository"),
-    commits_per_group: int = typer.Option(Config.commits_per_group, "--commits-per-group", help="Number of commits to include in each group (minimum, except the last group)"),
+    min_commits: int = typer.Option(Config.min_commits, "--min-commits", help="Group commits by number"),
     min_diff_lines: int | None = typer.Option(Config.min_diff_lines, "--min-diff-lines", help="Minimum diff lines threshold"),
     model_provider: str = typer.Option(Config.model_provider, "--model-provider", help="Model provider (openai or openrouter)"),
     model: str = typer.Option(Config.model, "--model", help="Model name"),
 ):
     """Generate AI-based git memory for the specified repository."""
     typer.secho(f"Repository: {repo_path}", fg=typer.colors.GREEN)
-    typer.secho(f"Group commits by: {commits_per_gro
... [truncated] ...
```

---

## Commit 0bb7bda: refactor: Remove commit grouping and --min-commits option

**Author:** dominIk (aider)
**Date:** 2025-05-18 17:55:30
**Lines changed:** 367

```diff
@@ -8,15 +8,15 @@
 
 ## **Project Summary**
 
-`git-memory` is a Python CLI tool and library that walks through a Git repository’s history to generate a detailed memory (`memory.md`), a structural diagram (`structure.mmd`), and a patch (`history.md`) per commit.
+`git-memory` is a Python CLI tool and library that walks through a Git repository’s history to generate a detailed memory (`memory.md`), a structural diagram (`structure.mmd`), and a patch (`history.md`) per commit (or commit group). 
 It integrates with AI providers (like OpenAI) to generate summaries and visual diagrams that reflect the evolution of the project.
-Each commit gets:
+Each commit (or group of commits) gets:
 
 * a natural-language memory (`memory.md`)
 * a Mermaid diagram (`structure.mmd`)
 * a history file with the patches (`history.md`)
 
-Each commit for these files should capture:
+Each commit (or related group of commits) for these files should capture:
 
 * **removal** of deprecated data.
... [truncated] ...
```

---

## Commit 8c7abc5: feat: Clean slate - remove all Python files for fresh start

**Author:** dominIk
**Date:** 2025-06-16 20:10:32
**Lines changed:** 897

```diff
@@ -1,16 +1,15 @@
- # Byte-compiled / optimized / DLL files
- __pycache__/
- *.py[cod]
- # Distribution / packaging
- *.egg-info/
- dist/
- build/
- # Virtual environments
- .env
- .venv
- # History output
- .history/
- # Editor directories and files
- .vscode/
- .idea/
-.aider*
+ # Byte-compiled / optimized / DLL files
+ __pycache__/
+ *.py[cod]
+ # Distribution / packaging
+ *.egg-info/
+ dist/
+ build/
+ # Virtual environments
+ .env
+ .venv
+ # History output
+ .history/
+ # Editor directories and files
+ .vscode/
+ .idea/
\ No newline at end of file
@@ -1,3 +0,0 @@
-Memory for commit b9e00dd: Initial commit
-
-[Diff details...]
\ No newline at end of file
@@ -1,3 +0,0 @@
-Structure diagram for commit b9e00dd
-
-[Mermaid diagram...]
\ No newline at end of file
@@ -1,8 +0,0 @@
-# Default ignored files
-/shelf/
-/workspace.xml
-# Editor-based HTTP Client requests
-/httpRequests/
-# Datasource local storage ignored files
-/dataSources/
-/dataSources.local.xm
... [truncated] ...
```

---

